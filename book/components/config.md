# Config 组件

Config 组件提供了用于定义和管理 PHP 应用程序配置选项的工具。它允许你：

* 定义配置结构、验证规则、默认值和文档；
* 支持不同的配置格式（YAML、XML、INI 等）；
* 将来自不同来源的多个配置合并为单一配置。

> **注意：** 你无需使用此组件来配置 Symfony 应用程序。请阅读有关[如何配置 Symfony 应用程序](configuration.md)的文档。

## 安装

```terminal
$ composer require symfony/config
```

## 了解更多

